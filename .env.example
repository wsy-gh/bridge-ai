# Anthropic API
ANTHROPIC_API_KEY=sk-ant-xxx

# Server
PORT=3000
NODE_ENV=development

# Session
SESSION_SECRET=change-me-to-a-random-string

# Database (SQLite path, or PostgreSQL connection string for production)
DATABASE_URL=./data/bridge.db
