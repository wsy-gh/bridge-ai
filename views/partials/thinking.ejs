<div class="thinking-indicator" id="thinking-<%= pendingMessage.id %>"
     data-chat-id="<%= chat.id %>"
     data-message-id="<%= pendingMessage.id %>">
  <p><strong>Thinking...</strong></p>
  <p class="text-small text-muted">Claude is generating a response.</p>
</div>
